{
  "name": "interactive-graphviz-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "test": "bun test",
    "build": "bun build server.ts --target=bun --outdir ../dist/server"
  },
  "devDependencies": {}
}
